# CLAUDE.md — equip-sync-g-module

이 레포의 설계·운영 문서와 코드 설명은 비공개 레포 `dps-store` 에서 관리한다.
워크스페이스 루트(두 레포의 상위 디렉토리)에서 세션을 열어 그쪽 문서를 먼저 본다.

> ⚠ **이 레포는 공개(PUBLIC)다.** 코드 주석·docstring·커밋 메시지에 설계 이유, 사고 이력, 벤더 내부 동작,
> 내부 문서 경로를 적지 않는다. 설명이 필요하면 `dps-store` 문서에 쓴다.
